.env
bin/
tmp/
*.test
*.out

# `go build ./cmd/app` from the root drops the binary here.
/app

# Local module resolution while the framework is not published.
go.work
go.work.sum

.DS_Store
# SQLite lives here in development. The file is never committed.
database/*.sqlite
database/*.sqlite-shm
database/*.sqlite-wal

# SQLite lives here in development. The file is never committed.
database/*.sqlite
database/*.sqlite-shm
database/*.sqlite-wal
